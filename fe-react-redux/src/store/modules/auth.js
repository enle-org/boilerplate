// import APi request functions from Api folder

// import helpers

const DEFAULT_STATE = {
  error: {},
};

export const authReducer = (state = DEFAULT_STATE, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
